import 'package:flutter/material.dart';

class FavoriteScreens extends StatelessWidget {
  const FavoriteScreens({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Sevimlida ovqat mavjud emas"),
    );
  }
}
